inline def clamp(in value, in minVal, in maxVal)
    return max(minVal, min(value, maxVal));
end;

inline def mapRange(in value, in inMin, in inMax, in outMin, in outMax)
  return outMin + ((value - inMin) * (outMax - outMin)) / (inMax - inMin);
end;